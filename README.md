# Karatsuba Multiplication Algorithm

Мини-проект по курсу «Алгоритмы и структуры данных».

## Обзор проекта

В данном проекте реализован алгоритм умножения Карацубы и выполнено его сравнение с классическим методом умножения.

Алгоритм Карацубы основан на принципе «разделяй и властвуй». Вместо выполнения четырёх рекурсивных умножений он выполняет только три, что позволяет уменьшить асимптотическую сложность с:

**Наивное умножение:** O(n²)

до

**Алгоритм Карацубы:** O(n^1.585)

---

## Структура проекта

```text
mini-project-The-Karatsuba-Algorithm/

├── karatsuba/
│   ├── __init__.py
│   ├── karatsuba.py
│   └── naive.py
│
├── tests/
│   ├── test_karatsuba.py
│   ├── benchmark.py
│   ├── recursion_demo.py
│   └── complexity_demo.py
│
└── README.md
```

---

## Реализация

Проект состоит из двух основных компонентов.

### Алгоритм Карацубы

Файл:

```text
karatsuba/karatsuba.py
```

Особенности реализации:

* рекурсивное умножение по методу «разделяй и властвуй»;
* поддержка отрицательных чисел;
* подсчёт количества рекурсивных вызовов;
* возможность проверки результатов сравнением со встроенным умножением Python.

### Наивное умножение

Файл:

```text
karatsuba/naive.py
```

Содержит реализацию классического школьного алгоритма умножения, используемого для сравнения с алгоритмом Карацубы.

---

## Тестирование

Модульные тесты расположены в файле:

```text
tests/test_karatsuba.py
```

Проверяются следующие случаи:

* умножение на ноль;
* умножение на единицу;
* небольшие заранее известные примеры;
* большие заранее известные примеры;
* отрицательные числа;
* случайно сгенерированные тестовые данные.

Запуск тестов:

```bash
python -m unittest tests.test_karatsuba
```

Результат выполнения:

```text
.......
----------------------------------------------------------------------
Ran 7 tests in 0.022s

OK
```

Все тесты были успешно пройдены, что подтверждает корректность реализации алгоритма.

---

## Измерение производительности

Для сравнения времени работы алгоритмов используется файл:

```text
tests/benchmark.py
```

Запуск:

```bash
python -m tests.benchmark
```

Пример результата:

```text
Digits    Naive (s)      Karatsuba (s)

100       0.000096       0.001187       
500       0.001975       0.014388       
1000      0.011626       0.043318       
2000      0.070689       0.211880       
5000      0.791940       0.684875       
10000     4.507000       1.864495
```

Для чисел небольшой длины наивная реализация работает быстрее алгоритма Карацубы из-за накладных расходов на рекурсивные вызовы и разбиение чисел на части. Однако с увеличением размера входных данных влияние этих расходов уменьшается, и алгоритм Карацубы начинает демонстрировать преимущество благодаря своей лучшей асимптотической сложности.

---

## Демонстрация рекурсивной работы алгоритма

Для демонстрации рекурсивной природы алгоритма используется файл:

```text
tests/recursion_demo.py
```

Запуск:

```bash
python -m tests.recursion_demo
```

Результат:

```text
Результат: 1082152022374638
Рекурсивные вызовы: 49
```

Полученный результат подтверждает корректность вычислений и показывает, что задача разбивается на множество рекурсивных подзадач.

---

## Анализ количества рекурсивных вызовов

Для исследования роста числа рекурсивных вызовов используется файл:

```text
tests/complexity_demo.py
```

Запуск:

```bash
python -m tests.complexity_demo
```

Результат:

```text
количество цифр = 4, количество рекурсивных вызовов = 16
количество цифр = 8, количество рекурсивных вызовов = 49
количество цифр = 16, количество рекурсивных вызовов = 175
```

С увеличением размера входных данных количество рекурсивных вызовов возрастает, что наглядно демонстрирует рекурсивную природу алгоритма Карацубы и его стратегию разделения задачи на подзадачи меньшего размера.

---

## Идея алгоритма

Пусть даны два числа:

```text
x = a · 10^m + b
y = c · 10^m + d
```

В классическом рекурсивном подходе необходимо вычислить четыре произведения:

```text
ac, ad, bc, bd
```

Алгоритм Карацубы сокращает их количество до трёх:

```text
z0 = bd
z2 = ac
z1 = (a + b)(c + d) − z2 − z0
```

После этого результат восстанавливается по формуле:

```text
x · y = z2 · 10^(2m) + z1 · 10^m + z0
```

Такой подход приводит к рекуррентному соотношению:

```text
T(n) = 3T(n/2) + O(n)
```

что даёт асимптотическую сложность:

```text
O(n^1.585)
```

---

## Заключение
В ходе выполнения проекта был реализован алгоритм умножения Карацубы и проведено его сравнение с наивным алгоритмом умножения.

Корректность реализации была подтверждена с помощью набора модульных тестов, включающего как заранее известные примеры, так и случайно сгенерированные входные данные. Все тесты были успешно пройдены.

Экспериментальное исследование показало, что для чисел небольшой длины алгоритм Карацубы уступает наивному алгоритму по времени выполнения из-за дополнительных затрат на рекурсивные вызовы и разбиение чисел на части. Однако при увеличении размера входных данных преимущество алгоритма Карацубы становится заметным.

Результаты измерений показали, что для чисел длиной 5000 цифр алгоритм Карацубы уже начинает превосходить наивный подход, а для чисел длиной 10000 цифр выполняется более чем в два раза быстрее. Это подтверждает теоретическое преимущество алгоритма и его лучшую асимптотическую сложность.

Таким образом, поставленная цель проекта была достигнута: алгоритм Карацубы был успешно реализован, протестирован и экспериментально исследован. Полученные результаты демонстрируют практическую эффективность метода «разделяй и властвуй» при работе с большими целыми числами.